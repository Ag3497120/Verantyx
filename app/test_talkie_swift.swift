import Foundation
import MLX
import MLXLMCommon

// This script needs to be compiled within the VerantyxIDE package context.
// Wait, I can't easily compile an executable that links against the VerantyxIDE targets unless I add an executable target.
