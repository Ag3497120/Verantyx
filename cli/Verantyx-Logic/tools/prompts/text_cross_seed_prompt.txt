You are generating a JSONL dataset.
Each line MUST be a valid JSON object with keys "id" and "text".
Output exactly 100000 lines.

Rules:
- "id" format: seed_000001, seed_000002, ... seed_100000
- "text" must contain exactly one quoted formula using double quotes "..."
- Do NOT output any extra text.
- Use mixed Japanese and English natural language templates.
- Use formulas from these categories: propositional logic, modal logic, linear algebra.

Examples of valid lines:
{"id":"seed_000001","text":"命題論理で次は恒真か？ \"((A -> B) & A) -> B\""}
{"id":"seed_000002","text":"In modal logic, is this valid? \"[]p -> [][]p\""}
{"id":"seed_000003","text":"実n次対称行列の次元は？ \"dim Sym(n,R)\""}

Now generate the dataset.
